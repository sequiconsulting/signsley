const { injectChainAndSigningTime } = require('./helpers/enrich-result');

// After building final result
function buildUltraComprehensiveResult(signatureInfo, parseResult, certInfo, validationResults, fileName, parsingLog, detailedLog, startTime) {
  const processingTime = Date.now() - startTime;
  // ... existing result build ...
  const result = {
    // previous fields retained
  };
  // Enrich with chain and signing time
  return injectChainAndSigningTime(result, parseResult);
}
